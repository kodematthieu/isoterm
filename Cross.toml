[target.aarch64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture arm64",
    "apt-get update",
    "apt-get install -y libssl-dev:arm64",
]

[target.aarch64-linux-android]
image = "messense/rust-musl-cross:aarch64-linux-android"
